---
trigger: always_on
---

When you need to search the web, if you are not able to use your search web tool, use the browser subagent instead. Also use the read_url_content (or similar) tool when you can.